- !new_status
  name: ReadyToShoot
  params:
    - shoot_target
  on_phase_end:
    - !eff
      name: LaunchAction
      action_name: ShootBullet
      setup:
        target: !area
          name: Cone60
          source: !prop carrier.cell
          target: !prop status.shoot_target
          length: 5
    - !eff
      name: RemoveStatus
      status: !status
        name: ReadyToShoot